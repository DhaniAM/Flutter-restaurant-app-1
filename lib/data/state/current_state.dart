enum HomeCurrentState { loading, noData, hasData, error }

enum RestoDetailState { loading, noData, hasData, error, hasDataFromNotif }

enum SearchCurrentState { init, loading, noData, hasData, error }

enum FavoriteState { noData, hasData, error }

enum FavoriteListState { loading, hasData, noData, error }

enum DatabaseState { loading, hasData, noData, error }
